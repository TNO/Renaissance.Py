[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "renaissance-refactor"
version = "0.1.0"
description = "Python "
readme = "README.md"
authors = [
    {name = "Luna Li", email = "luna.li@capgemini.com"}
]
license = {text = "MIT"}
requires-python = ">=3.13"
dependencies = [
    # List your dependecies here
    "textx",
    "dataclasses-json",
    "clang>=18.1.8",
    "libclang",
    "parameterized",
    "coverage",
    "pyperclip"
]